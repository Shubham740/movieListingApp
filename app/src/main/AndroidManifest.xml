<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.assignment.root.movielistingapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.assignment.root.movielistingapp.application.RequestManagerInitialise">
        <activity
            android:name="com.assignment.root.movielistingapp.activity.MovieListAppActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.assignment.root.movielistingapp.activity.VideoPlayActivity"
            android:launchMode="singleInstance"
            android:configChanges="orientation|orientation|screenSize|keyboardHidden" />
    </application>

</manifest>